# Sync upstream changes

You can sync remote changes for the bootstrapped app by running the following commands:

```shell
cd ./my_superapp;
cd ./superapp/apps/your_app_name_here;
django_superapp pull_template .
```

