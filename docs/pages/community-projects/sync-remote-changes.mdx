# Sync upstream changes

You can sync remote changes for the bootstrapped project by running the following commands:

```shell
cd ./my_superapp;
cd ./superapp/apps/your_app_name_here;
django_superapp update .
```

